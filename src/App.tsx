import React, { FC } from 'react';
import './App.css';
import { ViewColumns } from './ViewColumns';

export const App: FC = () => {
  return (
    <div className="App">
      <ViewColumns />
    </div>
  );
};
